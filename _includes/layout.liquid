<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
  <header>
    <a href="/">My Test 11ty Site!</a></header>
    <a href="/characters">Rick and Morty</a></header>
  <main>
    {% if title %}<h1>{{ title }}</h1>{% endif %}
    {{ content }}
    {% if puppers %}<img src="{{ puppers }}" alt="random dog ceo"/>{% endif %}
  </main>
</body>
</html>