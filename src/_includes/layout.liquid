<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
  <header>
    <a href="/">My Test 11ty Site!</a>
    <img src="{{settings.banner }}" alt="site banner">
    <p>{{ settings.about }}</p>
  </header>
  <main>
    {{ content | safe }}
  </main>
</body>
</html>