<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body>
  <header>
    <a href="/">My Test 11ty Site!</a>
  </header>
  <main>
    <h1>{{ title }}</h1>
    <h2>{{postTitle}}</h2>
    <img src="{{image}}" alt="{{imageAlt}}">
    {% if puppers %}<img src="{{ puppers }}" alt="random dog ceo" width="250px" height="auto">{% endif %}
  </main>
</body>
</html>